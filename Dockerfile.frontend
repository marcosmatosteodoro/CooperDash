# Dockerfile.frontend
FROM node:20-alpine AS builder

WORKDIR /app
COPY frontend/ .

RUN npm install && npm run build

# --- Produção ---
FROM node:20-alpine

WORKDIR /app
COPY --from=builder /app ./

EXPOSE 3000

CMD ["npm", "start"]
